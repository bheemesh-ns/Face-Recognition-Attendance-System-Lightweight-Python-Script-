# Face-Recognition-Attendance-System-Lightweight-Python-Script-
A standalone Python + OpenCV + face_recognition library project for marking attendance. It automatically detects and recognizes registered faces from the known_faces folder, logs attendance to a CSV file, and prevents duplicate marking for the same day.
